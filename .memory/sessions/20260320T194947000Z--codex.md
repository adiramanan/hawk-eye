---
sessionId: "20260320T194947000Z--codex"
agent: "Codex"
startedAt: "2026-03-20T19:49:47.000Z"
endedAt: "2026-03-20T19:52:00.000Z"
status: "closed"
branch: "dev"
goal: "Apply the Figma input-container design to the inspector's shared input fields."
touched: ["packages/client/src/controls/NumberInput.tsx","packages/client/src/styles.ts","tests/client-ui-controls.test.ts",".memory/sessions/20260320T194947000Z--codex.md",".memory/CURRENT_CONTEXT.md",".memory/receipts.jsonl"]
---

## Context

- Reviewed `.memory/CURRENT_CONTEXT.md`, `.memory/MEMORY.md`, `.memory/BLOCKERS.md`, and `.memory/PHASE_STATUS.md` before starting.
- Figma node `119:1335` in `CraftKit` defines a dark 32px input container with optional prefix text, trailing unit text, and an optional compact unit modifier chevron.
- The repo currently has shared `text-input` and `select-input` primitives plus numeric field wrappers in `NumberInput.tsx`, but the unit-bearing number fields are still visually split across separate controls instead of a single Figma-style container.

## Actions

- Refactored unit-bearing number inputs to render inside a single `number-input-shell` so value entry, prefix scrubbing labels, trailing unit text, and unit selectors match the Figma input-container composition.
- Updated the shared input styling to use the Figma text sizing and spacing, added shell-specific overrides for inline unit selectors and scrub labels, and kept dirty/invalid state styling working on the new container.
- Added a regression test confirming multi-unit and single-unit number inputs now render inside one shared shell and verified the change with `pnpm type-check` and `pnpm test -- tests/client-ui-controls.test.ts tests/design-tool.test.ts`.

## Decisions

- Leave `SizeInput` on its existing custom implementation for now; apply this Figma node only to the shared text/select/number primitives that already map to the input-container behavior.

## Open Threads

- None.

## Summary

Applied the Figma input-container design to the shared inspector input primitives and verified the updated DOM/styling with type-check and tests.
