---
sessionId: "20260322T161912000Z--codex"
agent: "Codex"
startedAt: "2026-03-22T16:19:12.000Z"
endedAt: "2026-03-22T16:19:12.000Z"
status: "closed"
branch: "dev"
goal: "Fix the stale demo import path so inspector shell changes reach the running app."
touched:
  - "demo/vite.config.ts"
  - ".memory/sessions/20260322T161912000Z--codex.md"
  - ".memory/CURRENT_CONTEXT.md"
  - ".memory/receipts.jsonl"
---

## Context

- The user reported the removed inspector source/path label was still visible in the live demo.
- Investigation showed the demo Vite config aliased `hawk-eye` and `hawk-eye/vite` to `packages/hawk-eye/dist/*`, so source edits could pass tests without reaching the running app.

## Actions

- Updated `demo/vite.config.ts` so local `vite serve` uses `packages/hawk-eye/src/*` aliases while non-serve builds can still point at `dist`.
- Rebuilt the `hawk-eye` package bundle with `pnpm -F hawk-eye build` so the currently running demo has an updated `dist` payload immediately.
- Re-ran `pnpm type-check` and verified the rebuilt bundle no longer contains the `panel-source` runtime node.

## Decisions

- Local demo development should prefer source aliases to avoid repeating stale-bundle mismatches after UI changes.

## Open Threads

- The running dev server may still need a page refresh or panel reopen if it has an already-mounted stale inspector instance in memory.

## Summary

Fixed the demo import path so local dev uses source entrypoints, rebuilt the current bundle, and removed the stale delivery path that kept the old inspector source label visible.
