---
sessionId: "20260327T172835000Z--codex"
agent: "Codex"
startedAt: "2026-03-27T17:28:35.000Z"
endedAt: "2026-03-27T17:29:17.000Z"
status: "closed"
branch: "dev"
goal: "Make the selection overlay border-only so it does not obscure the selected element."
touched: [
  "packages/client/src/Inspector.tsx",
  "packages/client/src/styles.ts",
  "tests/design-tool.test.ts"
]
---

## Context

- The current overlay uses a translucent fill and a page-wide shadow wash, which makes it harder to inspect the selected element's actual colors.

## Actions

- Removed the translucent outline fill and the page-wide overlay shadow from the selection outline styles.
- Simplified the selected-outline render path so it uses the shared border-only outline style directly.
- Added a focused test assertion to keep the outline transparent and prevent the 9999px mask shadow from returning.
- Verified the change with `pnpm vitest --run tests/design-tool.test.ts`.

## Decisions

- Use border-only overlay treatment for hover and selected states.

## Open Threads

- None yet.

## Summary

The selection and hover overlays now use border-only highlighting, making the underlying element colors fully visible.
