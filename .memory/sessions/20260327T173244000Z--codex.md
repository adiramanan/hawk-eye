---
sessionId: "20260327T173244000Z--codex"
agent: "Codex"
startedAt: "2026-03-27T17:32:44.000Z"
endedAt: "2026-03-27T17:33:04.000Z"
status: "closed"
branch: "dev"
goal: "Fix dash/gap parsing so arrow-key updates do not surface comma-separated serialization in the dash field."
touched: [
  "packages/client/src/PropertiesPanel.tsx",
  "tests/design-tool.test.ts"
]
---

## Context

- The dash field can show `1,` after arrow-key updates because `stroke-dasharray` may be serialized back as comma-separated text like `1, 4`.

## Actions

- Updated the split dash/gap parser to handle both whitespace- and comma-separated `stroke-dasharray` serialization.
- Replaced the regression with an arrow-key test so the dash field stays numeric after keyboard stepping.
- Verified the fix with `pnpm vitest --run tests/design-tool.test.ts`.

## Decisions

- Parse split dash/gap values using comma-or-whitespace separators.

## Open Threads

- None yet.

## Summary

Arrow-key updates no longer leak comma-separated `stroke-dasharray` serialization into the dash input field.
