---
sessionId: "20260327T183041000Z--codex"
agent: "Codex"
startedAt: "2026-03-27T18:30:41.000Z"
endedAt: "2026-03-27T18:31:18.000Z"
status: "closed"
branch: "dev"
goal: "Fix width fill handling by preserving width and height keyword baselines such as auto and fit-content."
touched: [
  "packages/client/src/drafts.ts",
  "tests/drafts.test.ts"
]
---

## Context

- Reviewed the existing size-mode logic and the focused fill tests in `tests/design-tool.test.ts`.
- Found that `createSelectionDraft` still sanitizes width and height baselines as generic numeric controls, which drops keyword values used by fill and hug sizing.

## Actions

- Preserved non-numeric width and height baselines such as `auto` and `fit-content` during draft creation instead of sanitizing them to empty strings.
- Added regression coverage for authored `%` sizing and for width/height keyword baselines used by fill and hug modes.
- Verified the change with `pnpm vitest --run tests/drafts.test.ts` and `pnpm vitest --run tests/design-tool.test.ts -t "fill|hug|width|height"`.

## Decisions

- Width and height family properties should preserve their keyword baselines so fill and hug sizing can round-trip through draft creation, reset, and re-selection correctly.

## Open Threads

- Manual browser verification is still useful for live app scenarios where width fill is applied to existing specimens with more complex parent layout constraints.

## Summary

Width fill and hug-related sizing now keep their underlying keyword baselines during draft creation, with focused regression coverage in place.
