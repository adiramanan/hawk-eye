---
sessionId: "20260327T190846000Z--codex"
agent: "Codex"
startedAt: "2026-03-27T19:08:46.000Z"
endedAt: "2026-03-27T19:12:54.000Z"
status: "closed"
branch: "dev"
goal: "Hide width and height from the v1 inspector UI while keeping the underlying size logic available for a later v2 return."
touched: [
  "packages/client/src/PropertiesPanel.tsx",
  "packages/client/src/types.ts",
  "tests/design-tool.test.ts"
]
---

## Context

- The focused inspector still renders width and height plus aspect-ratio lock in the Size & Spacing section.
- The requested change is scope-only for v1: hide those controls from the UI without deleting the underlying size-mode behavior.

## Actions

- Removed width and height from the `V1_PROPERTIES` allowlist so they are no longer part of the approved v1 surface.
- Removed the explicit width, height, and aspect-ratio controls from the Size & Spacing section while keeping the underlying size-mode logic in place.
- Center-aligned the empty-state icon and copy and constrained the body width so the empty panel reads as an intentional centered state.
- Updated focused inspector tests to assert the new v1 contract and reran `pnpm vitest --run tests/design-tool.test.ts`.

## Decisions

- Keep width and height draft/state logic intact and only remove the visible v1 controls.

## Open Threads

- Width and height sizing logic remains in the codebase for a later v2 return, but the v1 panel no longer exposes it.

## Summary

Width and height are hidden from the v1 inspector UI, and the empty-state content is now centered without removing the underlying size implementation needed for v2.
