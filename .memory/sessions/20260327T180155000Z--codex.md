---
sessionId: "20260327T180155000Z--codex"
agent: "Codex"
startedAt: "2026-03-27T18:01:55.000Z"
endedAt: "2026-03-27T18:10:53.000Z"
status: "closed"
branch: "dev"
goal: "Fix width and height editing so authored size values are preserved instead of collapsing to computed pixels."
touched: [
  "packages/client/src/drafts.ts",
  "tests/drafts.test.ts"
]
---

## Context

- Reviewed `.memory/CURRENT_CONTEXT.md`, `.memory/MEMORY.md`, `.memory/BLOCKERS.md`, and `.memory/PHASE_STATUS.md` before changing the inspector.
- Traced the width/height path through `PropertiesPanel`, `SizeInput`, `DesignTool`, and `drafts`.
- Focused on selection-draft creation after confirming the existing width tests do not cover authored relative sizes.

## Actions

- Updated draft creation to prefer authored inline values for size properties before falling back to computed values.
- Added a regression test that mocks computed pixel sizes while preserving authored inline percentages to keep width and height relative sizing intact.
- Verified the fix with `pnpm vitest --run tests/drafts.test.ts` and `pnpm vitest --run tests/design-tool.test.ts -t "width|height|aspect ratio"`.

## Decisions

- Preserve authored size values for width and height during draft creation so the inspector does not flatten relative sizing to computed pixels on selection.

## Open Threads

- Manual browser verification is still useful for class-authored relative sizes, since the new test covers inline-authored size values.

## Summary

Width and height drafts now keep authored size values instead of replacing them with computed pixels, and the focused regression coverage is in place.
