---
sessionId: "20260320T194141000Z--codex"
agent: "Codex"
startedAt: "2026-03-20T19:41:41.000Z"
endedAt: "2026-03-20T19:43:18.000Z"
status: "closed"
branch: "dev"
goal: "Translate the Figma linked property input group into the repo's per-side control."
touched: ["packages/client/src/styles.ts",".memory/sessions/20260320T194141000Z--codex.md",".memory/CURRENT_CONTEXT.md",".memory/receipts.jsonl"]
---

## Context

- Reviewed `.memory/CURRENT_CONTEXT.md`, `.memory/MEMORY.md`, `.memory/BLOCKERS.md`, and `.memory/PHASE_STATUS.md` before starting.
- Figma node `120:1959` in `CraftKit` is a labelled, unlinked four-pill property input row with a broken-link toggle button.
- The existing repo implementation lives in `packages/client/src/controls/PerSideControl.tsx` with matching behavior but looser spacing and typography than the Figma component.

## Actions

- Matched the existing per-side control geometry to the Figma linked property input group by tightening the row layout, switching the four-pill spacing to `2px`, standardizing `32px` heights and `8px` padding, and aligning the value/unit typography to the Figma spec.
- Added focus-within styling to the per-side inputs and a visible focus state for the link toggle button so the control stays usable inside the repo's inspector shell.
- Verified the update with `pnpm type-check` and `pnpm test -- tests/design-tool.test.ts`.

## Decisions

- Keep the existing linked/unlinked interaction logic and icon treatment, but translate the Figma node's spacing, sizing, and typography into the current styling system instead of rebuilding the control from scratch.

## Open Threads

- None.

## Summary

Implemented the Figma per-side input group as a visual restyle of the existing control and verified the inspector still passes type-check and tests.
