---
sessionId: "20260327T163829000Z--codex"
agent: "Codex"
startedAt: "2026-03-27T16:38:29.000Z"
endedAt: "2026-03-27T16:46:38.000Z"
status: "closed"
branch: "dev"
goal: "Change the padding control from all-sides/each-side modes to opposite-sides/each-side modes."
touched:
  - "packages/client/src/controls/PerSideControl.tsx"
  - "packages/client/src/PropertiesPanel.tsx"
  - "packages/client/src/styles.ts"
  - "packages/client/src/controls/NumberInput.tsx"
  - "packages/client/src/controls/SizeInput.tsx"
  - "packages/client/src/controls/PerCornerControl.tsx"
  - "packages/client/src/utils/css-value.ts"
  - "tests/design-tool.test.ts"
  - ".memory/sessions/20260327T163829000Z--codex.md"
  - ".memory/receipts.jsonl"
---

## Context

- Reviewed `.memory/CURRENT_CONTEXT.md` before starting.
- Confirmed `PerSideControl` is shared by padding, margin, corner radius, and stroke weight.

## Actions

- Opened a session for the padding grouped-edit mode change.
- Updated `packages/client/src/controls/PerSideControl.tsx` to support a padding-only `opposite-each` grouping mode while keeping the existing two-mode toggle interaction.
- Wired the padding card in `packages/client/src/PropertiesPanel.tsx` to use `opposite-each`; other per-side controls continue using `all-each`.
- Added layout styling for the opposite-sides grouped inputs in `packages/client/src/styles.ts`.
- Updated `tests/design-tool.test.ts` to verify padding now edits vertical/horizontal pairs before toggling back to per-side inputs.
- Fixed numeric arrow-key stepping across shared controls by parsing the live input text before falling back to the last valid snapshot in `NumberInput`, `SizeInput`, `PerSideControl`, and `PerCornerControl`.
- Added `extractLooseNumber()` in `packages/client/src/utils/css-value.ts` so partially typed numbers like `1.` still step correctly.
- Extended `tests/design-tool.test.ts` with a keyboard regression for unit-aware numeric inputs.
- Verified the change with `pnpm vitest --run tests/design-tool.test.ts`.

## Decisions

- Scope the new opposite-sides mode to padding only so other grouped controls keep their current all-sides behavior.

## Open Threads

- None.

## Summary

Padding now replaces the old all-sides grouped mode with opposite-sides grouping, while all other per-side controls keep their original behavior. Numeric inputs also now step from the visible value instead of snapping to fallback values like `1`.
