---
sessionId: "20260327T173121000Z--codex"
agent: "Codex"
startedAt: "2026-03-27T17:31:21.000Z"
endedAt: "2026-03-27T17:31:41.000Z"
status: "closed"
branch: "dev"
goal: "Sanitize dash/gap inputs so invalid characters like commas do not remain visible."
touched: [
  "packages/client/src/PropertiesPanel.tsx",
  "tests/design-tool.test.ts"
]
---

## Context

- The split dash/gap UI for `strokeDasharray` currently allows invalid transient text such as `1,` to stay visible in the dash input.

## Actions

- Sanitized the split dash/gap values before rendering them through `NumberInput`, so invalid characters like commas do not remain visible.
- Added a focused regression test covering `1,` entered into the dash field.
- Verified the fix with `pnpm vitest --run tests/design-tool.test.ts`.

## Decisions

- Sanitize the split dash and gap snapshots before rendering them through `NumberInput`.

## Open Threads

- None yet.

## Summary

The split dash/gap controls now keep their visible values numeric, so invalid punctuation no longer leaks into the UI.
