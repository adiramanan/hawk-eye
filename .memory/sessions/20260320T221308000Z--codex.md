---
sessionId: "20260320T221308000Z--codex"
agent: "Codex"
startedAt: "2026-03-20T22:13:08.000Z"
endedAt: "2026-03-20T22:15:38.000Z"
status: "closed"
branch: "dev"
goal: "Fix the opacity control so the UI uses 1-100% semantics instead of displaying 0-1 values with a percent suffix."
touched:
  - "packages/client/src/editable-properties.ts"
  - "packages/client/src/drafts.ts"
  - "packages/client/src/controls/NumberInput.tsx"
  - "tests/design-tool.test.ts"
  - ".memory/sessions/20260320T221308000Z--codex.md"
  - ".memory/CURRENT_CONTEXT.md"
  - ".memory/receipts.jsonl"
---

## Context

- Reviewed the opacity property definition, numeric input component, and draft normalization path.
- The UI already labels opacity as `%`, but the draft layer still stores and displays raw CSS `0-1` values, so the control renders values like `0.8%`.

## Actions

- Updated the opacity property definition to use a `1..100` UI range while retaining the existing `%` unit display.
- Added opacity-specific translation in `NumberInput.tsx` so the control displays internal CSS opacity values like `0.8` as `80`, and routes keyboard/math input back through percentage semantics.
- Updated draft normalization so opacity input is converted from UI percent values into normalized CSS opacity decimals before preview/save.
- Added a DesignTool regression assertion covering visible opacity input `64` -> preview style `0.64`.
- Verified the change with `pnpm test -- tests/design-tool.test.ts` and `pnpm type-check`.

## Decisions

- Keep opacity stored and persisted as normalized CSS decimal values while presenting percentage semantics in the UI.

## Open Threads

- None.

## Summary

Fixed the opacity field so Hawk-Eye shows percentage values in the UI and converts them back to normalized CSS opacity values for preview and persistence.
