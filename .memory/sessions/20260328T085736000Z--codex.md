---
sessionId: "20260328T085736000Z--codex"
agent: "Codex"
startedAt: "2026-03-28T08:57:36.000Z"
endedAt: "2026-03-28T08:58:45.000Z"
status: "closed"
branch: "dev"
goal: "Decouple the demo chip styles so .he-chip and .he-status-chip each own their styling."
touched: [
  "demo/src/index.css",
  "demo/src/App.tsx"
]
---

## Context

- The demo stylesheet currently groups `.he-chip` and `.he-status-chip` into a shared base selector, so edits to the shared pill styling affect both elements.
- The runtime status chip markup also applies an inline text color, which partially overrides the class-based tone styling.

## Actions

- Removed `.he-chip` and `.he-status-chip` from the shared grouped selector so they no longer inherit the same pill-layout declarations implicitly.
- Duplicated the shared inline-flex, centering, full-radius, and nowrap properties directly into the dedicated `.he-chip` and `.he-status-chip` rules.
- Removed the inline `color` override from the runtime status-chip markup so the tone-specific CSS owns the final text color again.
- Checked the touched demo files for diagnostics; no linter errors were reported.

## Decisions

- Keep `.he-chip` and `.he-status-chip` visually similar where needed by repeating the base pill declarations in each rule instead of sharing a grouped selector.
- Let `.he-status-chip[data-tone=...]` determine status-chip text color rather than overriding it inline in JSX.

## Open Threads

- No automated runtime/browser pass was run for this small demo-only CSS refactor.

## Summary

The demo chip and status-chip styles are now decoupled, and the status chip is fully controlled by its own CSS selectors again.
