---
sessionId: "20260327T203845000Z--codex"
agent: "Codex"
startedAt: "2026-03-27T20:38:45.000Z"
endedAt: "2026-03-27T20:38:45.000Z"
status: "closed"
branch: "dev"
goal: "Bring corner-radius linking into parity with the fixed side-group normalization behavior."
touched:
  - "packages/client/src/controls/PerCornerControl.tsx"
  - "tests/client-ui-controls.test.ts"
  - "tests/design-tool.test.ts"
  - ".memory/CURRENT_CONTEXT.md"
  - ".memory/receipts.jsonl"
  - ".memory/sessions/20260327T203845000Z--codex.md"
---

## Context

- Reviewed the current grouped/each normalization behavior across padding, margin, corner radius, and stroke weight.
- Confirmed that padding, margin, corner-radius-in-panel, and stroke weight now route through the fixed `PerSideControl`, while the standalone `PerCornerControl` still lagged behind.

## Actions

- Updated `packages/client/src/controls/PerCornerControl.tsx` so switching into linked mode immediately applies the visible grouped value to every corner.
- Added a focused regression test for the corner-link behavior.
- Re-ran the targeted corner regression and the full focused design-tool suite.

## Decisions

- Corner linking should follow the same semantic rule as side linking: entering the linked/grouped state is an immediate commit, not a display-only toggle.
- The current normalization behavior is now aligned across the active grouped controls in the properties panel and the standalone corner control implementation.

## Open Threads

- The broader `tests/client-ui-controls.test.ts` file still contains unrelated pre-existing failures outside the targeted grouped-control regressions, so validation remains intentionally scoped.

## Summary

Corner linking now applies the visible grouped value immediately, bringing the remaining corner control path into parity with the fixed grouped side controls.
