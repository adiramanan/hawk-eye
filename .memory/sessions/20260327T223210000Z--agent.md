---
sessionId: "20260327T223210000Z--agent"
agent: "GPT-5.4 Nano"
startedAt: "2026-03-27T22:32:10.000Z"
endedAt: null
status: "open"
branch: null
goal: "Fix stale class-target preview propagation when switching selection between elements sharing the same authored class target."
touched:
  - "packages/client/src/DesignTool.tsx"
  - "tests/design-tool.test.ts"
---

## Context
Class-target preview was being cleared/re-rendered based on `selectedDraft`, causing stale styling when selecting a different element that shares the same class target.

## Actions
- Refactored `DesignTool` global class-target preview to be driven by all dirty class-target drafts (`pendingDrafts`), not only `selectedDraft`.
- Updated selection effect so switching elements only affects inline preview; global class-target `<style>` remains consistent.
- Added a regression test covering selection switch without re-analyzing the peer.

## Decisions
- Keep the global class-target preview as a single shared `<style>` element, but compute its CSS from all relevant drafts so selection changes do not wipe class-based rules.

## Open Threads
- Full suite `vitest --run` currently reports unrelated failures in `tests/client-ui-controls.test.ts`.

## Summary
Class-target preview propagation is fixed, with `tests/design-tool.test.ts` passing after the change.

