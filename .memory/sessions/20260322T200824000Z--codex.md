---
sessionId: "20260322T200824000Z--codex"
agent: "Codex"
startedAt: "2026-03-22T20:08:24.000Z"
endedAt: "2026-03-22T20:35:23.000Z"
status: "closed"
branch: "dev"
goal: "Debug why Apply fails instead of saving edits back to source."
touched:
  - "demo/vite.config.ts"
  - "packages/hawk-eye/src/installer.ts"
  - "packages/hawk-eye/README.md"
  - "packages/vite-plugin/src/index.ts"
  - "tests/installer.test.ts"
  - "tests/smoke.test.ts"
  - ".memory/sessions/20260322T200824000Z--codex.md"
  - ".memory/CURRENT_CONTEXT.md"
  - ".memory/receipts.jsonl"
---

## Context

- Reviewed `.memory/CURRENT_CONTEXT.md`, `.memory/MEMORY.md`, `.memory/BLOCKERS.md`, and `.memory/PHASE_STATUS.md`.
- Starting from branch `dev` with a clean worktree.
- Initial focus is the inspector Apply path across `packages/client` and `packages/vite-plugin`.

## Actions

- Inspected the client save flow in `packages/client/src/DesignTool.tsx` and `packages/client/src/Inspector.tsx`.
- Inspected the server-side save handler, websocket bridge, source injector, style analyzer, and source writer.
- Reproduced the live failure in the demo with Playwright and captured the exact panel error: `Could not find a JSX element at src/App.tsx:277:21.`
- Verified the underlying mismatch by reading the live heading token and confirming the clicked `Release readiness frame` `<h3>` was incorrectly stamped as `src/App.tsx:277:21` instead of its real source line.
- Patched the demo Vite config, installer, docs, and smoke coverage so `hawkeyePlugin()` is ordered before `react()`.
- Added a Vite startup warning when React transforms are ordered before Hawk-Eye source injection.
- Verified the fix with targeted vitest coverage, `pnpm type-check`, and a follow-up browser check showing the heading token corrected to `src/App.tsx:258:21`.

## Decisions

- The Apply failure was caused by plugin order, not the save handler itself.
- `hawkeyePlugin()` must run before `react()` so injected source coordinates match the original TSX file instead of React-refresh-transformed code.
- The installer should actively reorder `hawkeyePlugin()` to the front of the Vite plugins array when it already exists later in the list.

## Open Threads

- Manual consumer configs can still be misordered, but the plugin now emits a warning that explains the consequence and the required fix.

## Summary

Reproduced the Apply error to a mis-stamped source token caused by placing `react()` before `hawkeyePlugin()`, then fixed the demo, installer, docs, and plugin warning so source-write coordinates line up with the original file again.
