---
sessionId: "20260328T163500000Z--codex"
agent: "Codex"
startedAt: "2026-03-28T16:35:00.000Z"
endedAt: "2026-03-28T16:35:00.000Z"
status: "closed"
branch: "dev"
goal: "Fix reversed class-target property ownership so button background fill appears under the class that authors it."
touched: [
  "packages/client/src/drafts.ts",
  "packages/client/src/DesignTool.tsx",
  "tests/design-tool.test.ts",
  "tests/drafts.test.ts"
]
---

## Context

- The earlier button fix made `backgroundColor` show under `.he-button` instead of `.he-button-primary`.
- The real requirement is class ownership correctness, not a broad live-value fallback.

## Actions

- Replaced the broad button fallback with a narrower default-active-class heuristic for buttons that prefers the class target that authors `backgroundColor` or `color`.
- Removed the cross-target property fallback that was leaking radius/background values onto the wrong class target.
- Added and updated focused regressions for button multi-class selection and draft creation.
- Re-ran `pnpm vitest --run tests/drafts.test.ts tests/design-tool.test.ts`.

## Decisions

- Default class-target selection can be smarter for buttons, but property values must remain scoped to the active class target once selected.

## Open Threads

- None.

## Summary

Resolved the reversed button-property ownership issue by preferring the variant class target on first button selection while keeping background and radius values scoped to the class that actually authors them.
